import { ObraSocialPipe } from './obra-social.pipe';

describe('ObraSocialPipe', () => {
  it('create an instance', () => {
    const pipe = new ObraSocialPipe();
    expect(pipe).toBeTruthy();
  });
});
